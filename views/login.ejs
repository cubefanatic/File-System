<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta http-equiv="X-UA-Compatible" content="ie=edge">
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
	<title>Mongo File Upload</title>
</head>

<body>
	<div class="container" style="margin-top: 20%; margin-left: auto; margin-right: auto;">
		<form method='POST' action='/user/login'>
			<input id="what" type="hidden" name="what" value="1">
			<label for="username">Username</label>
			<input type="text" class="form-control" name="username" placeholder="Username">
			<label for="password">Username</label>
			<input type="password" name="password" class="form-control" placeholder="Password">
			<br>
			<input type="submit" name="submit" value="Submit" class="btn btn-primary mb-2">
		</form>
		Login / Register: <button id="toggle" onclick="toggle()" class="btn btn-primary mb-2">Login</button>
	</div>

	<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
	<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
	<script>
		function toggle() {
			let what = document.getElementById('what');
			let btn = document.getElementById('toggle');
			if(btn.innerText == "Register") location.reload();
			btn.innerText = what.value === 1 ? "Login" : "Register";
			what.value = 1 - what.value;
			// console.log(what.value);
			// console.log(btn.innerText);
		}
	</script>
</body>

</html>